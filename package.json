{
  "name": "@nickmeinhold/claude-slides",
  "version": "1.0.0",
  "description": "Generate Google Slides presentations from code review data",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "claude-slides": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "auth": "tsx src/cli.ts --auth",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "commander": "^13.0.0",
    "googleapis": "^166.0.0",
    "open": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.4.2",
    "@vitest/coverage-v8": "^4.0.18",
    "tsx": "^4.21.0",
    "typescript": "^5.2.2",
    "vitest": "^4.0.18"
  }
}
